{% extends 'base.html.twig' %}

{% block title %}RoomType{% endblock %}

{% block body %}
    <h1>RoomType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ room_type.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ room_type.description }}</td>
            </tr>
            <tr>
                <th>Price_markup</th>
                <td>{{ room_type.priceMarkup }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('room_type_index') }}">back to list</a>

    <a href="{{ path('room_type_edit', {'id': room_type.id}) }}">edit</a>

    {{ include('room_type/_delete_form.html.twig') }}
{% endblock %}
